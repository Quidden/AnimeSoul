[tool.ruff]
target-version = "py311"
line-length = 120
extend-exclude = [
  ".venv",
  "build",
  "dist",
  "frontend",
  "mobile/.toolchains",
  "mobile/android/build",
  "mobile/android/app/build",
]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
